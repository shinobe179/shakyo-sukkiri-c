#include <stdio.h>
#include <stdbool.h> // bool型を使う時に必要

int main(void)
{
	bool isMarried;
	isMarried = true;
	
	bool result;
	result = false;

	printf("isMarried: %d\nresult: %d\n", isMarried, result);
}
